# Erdős Problem 361 - 证明状态

## 问题

F_c(n) = max {|A| : A ⊆ {1,...,⌊cn⌋}, n ∉ Σ(A)}

## 已证明（Lean 4 编译通过，无 sorry）

### 定理 1：c < 1 的下界

**陈述**: 对于 k ≥ 2 且 k ∤ n，区间 [n/(k+1)+1, n/k] 中的任何子集都不能求和为 n。

**证明**: 对 |S| 分情况：
- |S| ≤ k: sum ≤ k·(n/k) ≤ n。若 sum = n 则 k | n，矛盾。
- |S| ≥ k+1: sum ≥ (k+1)·(n/(k+1)+1) > n，矛盾。

**推论**: F_c(n) ≥ ⌊n/k⌋ - ⌈n/(k+1)⌉

**状态**: ✅ 完整证明，无 sorry

### 定理 2：c ≥ 1 的下界

**陈述**: 对于 n ≥ 2，区间 [(n+1)/2, cn] \ {n} 中的任何子集都不能求和为 n。

**证明**:
- |S| = 0: sum = 0 ≠ n
- |S| = 1: 元素 ≠ n（因为 n 被排除）
- |S| ≥ 2: 任取两个不同元素 a, b ≥ (n+1)/2，则 a+b > n

**推论**: F_c(n) ≥ ⌊cn⌋ - ⌈n/2⌉

**状态**: ⚠️ 核心引理（two_elements_sum_gt_n）已证明，完整证明需要 Mathlib 的 List API

### 定理 3：c ≥ 1 的上界

**陈述**: 对于 c ≥ 1，任何 A ⊆ {1,...,cn} 且 |A| > cn - ⌈n/2⌉ 都包含一个子集求和为 n。

**证明**（配对论证）:
- 将 {1,...,cn} 划分为 G1 = {1,...,⌈n/2⌉-1}、G2 = {⌈n/2⌉,...,n-1}、G3 = {n+1,...,cn}
- 配对 {x, n-x} 将 G1 与 G2 连接
- Admissible 的 A 最多包含每个配对中的一个元素
- 最大大小 = |G3| + |G1| = cn - n + ⌈n/2⌉ - 1 = cn - ⌈n/2⌉

**状态**: ⚠️ 证明草图已文档化，完整形式化需要 Mathlib（Finset、Fintype、鸽巢原理）

## 综合结果

### c ≥ 1
- 下界: F_c(n) ≥ ⌊cn⌋ - ⌈n/2⌉
- 上界: F_c(n) ≤ ⌊cn⌋ - ⌈n/2⌉
- **结论**: F_c(n) = ⌊cn⌋ - ⌈n/2⌉

### c < 1
- 下界: F_c(n) ≥ ⌊n/k⌋ - ⌈n/(k+1)⌉（对于任意 k ≥ 2 且 k ∤ n）
- 上界: 需要进一步工作
- **结论**: F_c(n) = max over k≥2 of (⌊n/k⌋ - ⌈n/(k+1)⌉)（猜想）

## 诚实评估

### ✅ 已完成
1. c < 1 的下界证明（完整，无 sorry）
2. c ≥ 1 的下界核心引理（pair_sum_gt）
3. c ≥ 1 的上界证明草图

### ⚠️ 需要 Mathlib
1. c ≥ 1 的完整下界证明（需要 List.length_eq_one 等 API）
2. c ≥ 1 的完整上界证明（需要 Finset、Fintype、鸽巢原理）

### ❌ 未完成
1. c < 1 的上界证明
2. 最优 k 的选择
3. F_c(n) 的正式 Lean 定义
